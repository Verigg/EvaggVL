# **EvaggVL — агрегатор событий Владивостока**

EvaggVL — это веб-приложение на Python Flask, предназначенное для агрегирования и отображения событий, происходящих в городе Владивосток. Проект предоставляет удобный интерфейс для просмотра мероприятий, авторизации пользователей и работы с базой данных.

---

## 🚀 **Функции проекта**

* Просмотр списка событий Владивостока
* Авторизация и аутентификация пользователей (Flask-Login)
* Работа с базой данных через SQLAlchemy
* Удобный веб-интерфейс на Flask + Jinja2

---

## 📦 **Стек технологий**

* Python 3.10+
* Flask 3.1
* SQLAlchemy 2.0
* Flask-Login
* HTML/Jinja2

Полный список зависимостей — в `requirements.txt`.

---

## 🛠 **Локальный запуск проекта**

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/Verigg/EvaggVL
cd EvaggVL
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
```

### 3. Активируйте окружение

**Windows:**

```bash
venv\Scripts\activate
```

**Linux / macOS:**

```bash
source venv/bin/activate
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

### 5. Настройте переменные окружения

Для запуска Flask необходимо указать основной файл, например:

**Windows (PowerShell):**

```bash
$env:FLASK_APP="app.py"
$env:FLASK_ENV="development"
```

**Linux / macOS:**

```bash
export FLASK_APP=app.py
export FLASK_ENV=development
```

### 6. Запустите проект

```bash
flask run
```

После запуска приложение будет доступно по адресу:

👉 [http://127.0.0.1:5000/](http://127.0.0.1:5000/)